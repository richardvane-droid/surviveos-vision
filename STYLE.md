# SurviveOs 设想版网站 · 内容与插图规范（给各区域写手/画手）

## 0. 项目背景（一句话）
用户在杭州乡下自建一套"乡野生存系统 SurviveOs"：7 个物理区域 × 约 40 个子模块（木屋、地堡、阁楼、仓库、庭院、草地、钓台）。
本网站是 **0901 设想版**：天马行空、不严谨、只在模块框架内幻想每个模块"做完之后的完整形态"。
文案语气 = **小红书种草体**；插图 = **黑白钢笔速写**（统一风格，见第 2 节）。
智能生态默认：华为智慧生活 + HomeAssistant + ESPHome（可以顺口提，不必展开）。
用户有自己的 PSK（Personal Survival Kit，生存盒）产品线，暖村木屋兼作 PSK 展示间。

## 1. 文案：每个模块一个 JSON 对象（写到 data/0X.json，一个区域一个文件，数组）

小红书热门帖子的调性参考（2026 年小红书官方居住趋势）："适我主义"、"精神角落 / 精神领地 / 逃避间"、"痛屋（兴趣主题房间）"、"家的丰容计划 / 人类丰容"、"去家务化 / 动线"、"动手主义"、"自然生活 / 和植物一起住"、"观鸟"热潮；以及常见爆款句式："谁懂啊""沉浸式""氛围感拉满""保姆级""避坑""码住""被问爆了""一整个爱住""松弛感""低成本高质感""废墟风/露营风/原木风/工业风"。
标题要有钩子，正文短句、口语化、适量 emoji（每条 0~1 个，别堆）。**内容不用严谨，但要具体、有画面、有细节**（具体的物件、尺寸、动作、时刻）。

字段（全部必填，纯文本，不要 Markdown，不要 HTML）：

```json
{
  "id": "0103",
  "name": "电子画框墙",
  "hook": "谁懂啊！9 台二手 iPad 拼成一面会呼吸的画框墙",      // 小红书式标题，18~30 字
  "tagline": "一面每天早上自动换画的墙，Pinterest 就是我的策展人。",  // 一句话设想，25~45 字
  "scene": "……",            // 沉浸式想象：一段 120~200 字的场景描写，第一人称或第二人称，写"做完之后某个具体时刻"的画面（光线/声音/动作/温度）
  "plan": ["……", "……"],     // ✅ 设想方案清单：5~7 条，每条 20~45 字，写具体做法（材料/尺寸/布置/联动），像小红书"保姆级"分点
  "inspo": ["……", "……"],    // 🔥 小红书灵感点：3 条，每条 20~40 字，点名一种小红书上流行的做法/风格/话题并说明怎么用在这个模块
  "pitfalls": ["……", "……"], // ⚠️ 避坑：3 条，每条 20~40 字，口语化
  "budget": "一次性约 ¥3,000～6,000（9 台二手 iPad 占大头）；年度电费 ¥100 左右",  // 幻想预算，一句话，带范围
  "links": ["0211", "0201"], // 联动模块编号 1~3 个（真实存在的编号）
  "tags": ["露营风", "精神角落", "DIY"], // 3~5 个 # 标签词，不带 #
  "sketch_brief": "……"      // 给画手的一句话：这张速写画什么（主体+视角+1~2 个细节），30 字内
}
```

## 2. 插图：黑白钢笔速写 SVG（写到 illos/NNNN.svg，每模块一张）

**必须严格统一风格**，看 illos/_sample.svg 就是标准答案，照着它的手法画：

- 画布：`<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 400 300" width="400" height="300">`，不设背景（页面是米白纸色）。
- 只用**黑色墨线** `#1c1c1c`，不用任何彩色、不用灰色填充块（阴影全部用排线 hatching 表现）。允许 `fill="#fff"` 遮挡后方线条（比如前景物体挡住背景）。
- 线条：`stroke="#1c1c1c" fill="none" stroke-linecap="round" stroke-linejoin="round"`；主轮廓 `stroke-width` 1.6~2，细节 0.9~1.2，排线 0.7~0.9。
- **手绘感**：所有线条放进 `<g filter="url(#mNNNN-rough)">`（把 NNNN 换成模块编号，例如 `m0103-rough`），filter 定义照抄 sample（feTurbulence baseFrequency 0.04 + feDisplacementMap scale 1.6）。主轮廓可以用**两条略微错开的线**叠加一次（"描了两遍"的钢笔感），少量线头出头（overshoot 2~4px）。
- 阴影/材质：用 `<pattern>` 定义 45° 排线（sample 里有 `hatch` 和 `hatch2`（双向交叉）），暗部 `fill="url(#mNNNN-hatch)"`。木纹/砖缝/草叶用短线簇。
- 构图：一个清晰主体（占画面 60~75%），透视用简单的两点或一点透视，带一点环境（地面线、一两个小道具、几缕草或一棵树的局部），下方留出 8~12px 空白别贴边。画面里可以有 1~2 个"小人"（简笔火柴人+圆脑袋）给尺度感，可选。
- **右下角签名**：sample 里的 `<text>` 编号（如 `0103`），font 用 `font-family="'Comic Sans MS','Segoe Print','Bradley Hand',cursive"` 10px，照抄。
- 所有 id 必须以 `mNNNN-` 前缀（避免多张图同页冲突）。不要用外部资源、不要 `<image>`、不要脚本。
- 文件大小控制在 3~12 KB，路径数量 40~150 条，够看出是什么就行，宁可简洁清楚也不要糊成一团。
- 画什么以 JSON 里的 `sketch_brief` 为准。

## 3. 区域插图（每区域一张，illos/area-0X.svg）
同样风格，画这个区域的"全景/外观/剖面"，viewBox 0 0 600 300。

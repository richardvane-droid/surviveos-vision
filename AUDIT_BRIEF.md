# 0008 对齐审稿规范（给各区域"校对员 + 重绘员"）

背景：真实项目刚做出 `0008 户型图与空间标注`（`ref0008/0008.html`，事实表见 `SPACE_0008.md`）。畅想版此前很多模块的**位置、朝向、尺寸、相邻关系**是凭空想的，现在要全部对齐 0008；**铅笔画/钢笔画里凡是表现空间布局的，要尽可能接近 0008 的图**（`illos/plan-*.svg` 是 0008 五张图的铅笔版，可直接参考几何）。设想内容仍可放飞，但不能和 0008 打架。

## 你要做的事（按模块逐个过）

1. **读**：`SPACE_0008.md`（必读，含常见冲突清单）、`ref0008/0008.html`（细节）、`STYLE.md`（钢笔速写规范）、`PARTS_BRIEF.md`（铅笔拆解图规范）、`illos/_sample.svg`、`parts/_sample-*.svg`、`illos/plan-*.svg`。
2. **文案对齐** `data/0X.json` 每个模块：
   - 新增字段 `"place"`：60~120 字，一句到两句，写清它在哪个区域的哪一侧/哪个角、尺寸或占地、朝向、挨着什么（全部来自 0008；0008 没给位置的模块，按事实表里"只能塞进剩余空间"的原则自己定，并在句尾注明"（0008 未定位，设想落位）"）。预估数字前加 ≈。
   - 逐字段检查 hook / tagline / scene / plan / inspo / pitfalls / budget / sketch_brief，凡与 0008 冲突的改掉（面积、形状、窗、层高、房间位置、相邻关系、"独立房间 vs 柜子"、"池塘 vs 河"……）。改动要**最小且自然**，保留小红书语气；不要为了改而重写整段。
   - **全部按一个人设计**：删掉/改写 待客、客人、家人、朋友、招待、来客、访客、女儿、孩子 之类的表述（例如"客人喜欢当场就能带走"→"自己看腻了就换一只"；"来客感应亮灯"→"人靠近感应亮灯"）。part 名字里有的也要改。
   - `links` 只在明显错误时改。
3. **拆解文案对齐** `data/parts-0X*.json`：intro / points / impl / name 里与 0008 冲突或涉及待客的改掉。不改 kind/k 结构，不增删 part。
4. **重绘钢笔速写** `illos/NNNN.svg`（400×300，规范见 STYLE.md，id 前缀 `mNNNN-`）：只重绘**画面布局与 0008 冲突的**（例如木屋正中一张展台、地堡里有窗、阁楼是直墙房间、喷漆间是一个房间、钓台伸进水里、草地是方的、鱼缸在地堡……）。重绘时**构图尽量按 0008 的几何来**：正确的墙、正确的角、正确的相邻物件（例如画 0107 吧台，就是北墙西段 900 高矮柜 + 上方画框墙 + 右手边梯子床；画 0209，就是地堡西北角架空床下 1.2×1.5 帐篷，左边是西墙、上方床板 1750）。没冲突的不动。
5. **重绘区域速写** `illos/area-0X.svg`：区域图必须表现 0008 的真实空间形态（01：2×5 米长条剖面，北墙展示面、西段吧台、东墙梯子床、西南角柴火炉、南墙门窗；02：4.85×5 无窗房间的鸟瞰/剖面，床在西北、电视在南墙、门在东南；03/04：45° 坡顶下的阁楼，可站立核心区与低矮圈；05：木屋以南 22㎡ 小院，南端石墩、东邻草地；06：东侧 5.3×18.9 长条草原，乔木沿长边、雨水罐靠建筑东墙；07：缓坡尽头临河的 2.5×2.2 小平台，面前 10 米宽的河）。
6. **重绘铅笔拆解图** `parts/NNNN-K.svg`（及 `-poster.svg`）：只重绘**图里画了空间位置/尺寸且与 0008 冲突**的（例如 0201 滑轨要贴梁、净高 2400；0209 帐篷在床下 1.2×1.5；0107 吧台 900 高在北墙；0303 帐篷 2.35×2.16 在坡顶下；0403 是 1.5×1.0 喷涂柜不是排风墙+门框；0601 长条分区；0603 罐组靠东墙；0701 钢桩平台→岸上小平台 2.5×2.2；0702/0703 尺度缩到 5.5㎡……）。爆炸图里的零件本身（灯头、泵、传感器）不用动。保持 PARTS_BRIEF 的铅笔风格与 id 前缀。若 part 的 name 因此改了，图里的标题也要同步。
7. **自检**：
   - `python3 -c "import json;json.load(open('data/0X.json'))"`，parts 同理；中文字符串里不能有英文双引号（用“”）。
   - `python3 -c "import xml.dom.minidom as m,glob;[m.parse(f) for f in glob.glob('illos/0X*.svg')+glob.glob('illos/area-0X.svg')+glob.glob('parts/0X*.svg')]"`。
   - 每张重绘的图都用 `python3 render.py /tmp/xx.png a.svg b.svg …` 渲染后 **Read 看 PNG**：文字不压线、不出框、构图与 0008 一致；不合格就改。
   - 最后 `cd /home/claude/vision && python3 build.py` 必须成功。
8. **汇报**：列出（a）每个模块的 place 一句话；（b）改了哪些文案冲突（模块号 + 一句话）；（c）重绘了哪些 SVG 文件；（d）你不确定、留给主控裁决的点。不要贴整段 JSON。

## 硬规则
- 不要动 `build.py`、`SPACE_0008.md`、`illos/plan-*.svg`、其他区域的文件。
- 编辑 JSON 用 Python（`json.load` → 改 → `json.dump(..., ensure_ascii=False, indent=1)`）或精确 Edit，绝不手抄整文件。
- 图只用规定的灰黑墨线与排线，没有彩色、没有 `<image>`、没有脚本；id 前缀不能撞。
- 子代理如果被限流中断，先跑一遍自检，把已完成的列出来再结束。
